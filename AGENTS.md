# First Chorus — AIアシスタント向け指示(Codex / Claude / その他 共通)

**作業を始める前に、必ずリポジトリ直下の [HANDOFF.md](HANDOFF.md) を通読してください。**
プロジェクトの方針・アーキテクチャ・過去のバグから確立した設計原則・開発ワークフローの正本です。
このファイルは「最低限の要点」と「直近の引き継ぎ」だけを載せています。

最終更新: 2026-09-13(Claude → Codex への引き継ぎ)

## 現在地

- 公開アプリ: https://tanbo-hiroooi.github.io/Jazz-improvisation/ (`main` への push で GitHub Actions が自動デプロイ)
- 最新コミット: `5eed7c6 Simplify the pitch row: one octave pair and always-visible articulation`(公開済み)
- 作業開始時は `git status --short` と `git log -10 --oneline` で、この記述より新しい変更が無いか確認すること。

## 絶対に守る契約(詳細は HANDOFF.md §2・§10)

1. **見本演奏なし**: AI・乱数による見本ソロ/フレーズの生成と再生は禁止。鳴らしてよいのは「譜面に表示された固定譜例」と「ユーザー自身が編集したフレーズ」だけ。禁止用語:「見本を聴く」「お手本を再生」「Play Example」。
2. 内部の音程は常に**実音(Concert)MIDI**。移調は表示時のみ。五線譜・TAB・音名ボタン・再生音の音程は常に一致させる。
3. 練習コースのデータは `src/data/courses.ts`(純データ)。STEPの説明文と譜面・再生内容を**完全一致**させる。本文の `{bars}` `{minNotes}` `{minRest}` は実効値に差し込まれるので、数字を直書きしない。
4. 編集STEPの達成状態はstateにコピーせず、draftから**毎レンダー導出**する(`StepPractice.tsx` の `resolveEditable`)。
5. `GridPhrase`(保存形式)を変えない。新しい入力UIは既存の保存データ(`fc-saved-phrases-v1`)と互換であること。
6. 再生フック `usePracticePlayback` は「アクティブな画面/STEPに1つ」。集中モード用に別画面を作らない(過去に音が出ないバグ)。
7. 日本語・英語を同時に更新する(`src/i18n.ts` とデータ内の `Bi` 型)。
8. 録音機能は作らない(オーナー決定)。内部識別子(package名・localStorageキー・リポジトリ名・deploy workflow)は変えない。
9. コミットは「英語の要約1行+日本語の箇条書き本文」。コミット・push はオーナーの確認後。

## 変更を渡す前の手順(必須)

```bash
npx tsc --noEmit
npm run test:entry   # 入力・グリッド・スウィングに触れたら(gridEntry 1303件 + swing 12件)
npm run build
```

そのうえで実ブラウザ(`npm run dev` → localhost:5173)で対象操作を実際に行い、console error 0・幅390pxで横スクロール無しを確認する。
譜面の検証は「StaffView を裏で直接 render して `.vf-notehead` / `g.vf-tuplet` の bbox を測る」のが速い(HANDOFF §8 参照)。
コース全体に影響する変更は、全27レッスン・全51STEPを走査してから報告する。

## 2026-09-13 に完了した変更(Claude セッション、7コミット)

フレーズ編集(自由練習「フレーズを作る」・レッスンの編集STEP・章まとめ練習で共通の `GridEditor`)を
「全体譜の上で直接編集する」形に作り替えた。詳細は HANDOFF.md §8「音符の順番入力」。

| コミット | 内容 |
|---|---|
| `929ff6d` | **全体譜が編集面**。1小節の拡大欄を補助表示(「この小節を拡大」)に格下げ。休符タップ=入力位置、音符タップ=修正。編集中の小節だけ休符を拍ごとに分け(`entryFocusMeasure`)、他の小節の全休符はタップ位置から拍を割り出す |
| `9412fe0` | **ドラッグ**: 上下=高さ(パレットに吸い付く)、左右=位置(8分/16分/3連に吸い付く)。`gridEntry.moveNote` 追加。ドラッグ中は `dragGrid` の仮譜面、指を離した時だけ履歴へ |
| `75b25f8` | 当たり判定の `rect` に `stroke: none`(枠が見えていた)。全音符を追加。素材ピッカーを入力パネル内へ |
| `2a0339c` | 3連の数字を**符幹の側**、表情記号を**符頭の側**に置く(コード名との重なりを解消)。コード名は3連の後に描画。休符ボタンを音価の列へ |
| `1c6dad8` | **スウィング**を純関数 `theory/rhythms.ts::swingNotes` に切り出し、16分がある拍はイーブンに(16分のタイミングが崩れていた)。`tests/swing.test.ts` 追加 |
| `f2cb458` | 選んだ音を1タップで±1オクターブ(`shiftOctave`) |
| `5eed7c6` | 「音の高さ」の行を整理: 「⇧/⇩ 1オクターブ」1組(選択中はその音、未選択なら入力音域)+ 表情ボタン常設(未選択なら次の音に付く `pendingArtic`)。「−1/+1オクターブ」「▲▼」「この音の次から入力」を撤去 |

### いまの操作モデル(GridEditor)

- 音価(全/2分/4分/8分/16分/付点/休符)を選ぶ → 音名ボタンを押す → カーソル(オレンジの縦線)が進む。
- 譜面のどこでも: 休符タップ=そこから入力、音符タップ=修正モード(位置は進まない)、空き部分タップ=小節移動。
- 音符のドラッグ: 上下=高さ、左右=位置。音符にフォーカスして↑↓=1段。
- 「⇧/⇩ 1オクターブ」と表情ボタンは、音を選んでいればその音、選んでいなければ次に入力する音に効く。
- 「この拍を3連にする」で拍単位に3連へ(既存の音があればプレビュー→適用)。3連の拍をまたぐ音は不可。
- 修正モードから順番入力へ戻るのは、休符タップか「進む→」。

### 主な実装場所

- `src/components/GridEditor.tsx`: 全体譜(`fullScore`)、入力パネル、ドラッグ/オクターブ/表情のハンドラ、`snapTime`
- `src/components/StaffView.tsx`: VexFlow描画、休符/音符の当たり判定と `onDragNote`/`onNudgeNote`、3連・表情記号・コード名の位置、全体フィット(`fitHeight`/`fitMaxZoom`)
- `src/theory/gridEntry.ts`: 1拍=12tickの純ロジック(`enterNote` / `convertEntryBeat` / `moveNote`)
- `src/theory/rhythms.ts`: `swingNotes`(再生とハイライトが同じ結果を使う)
- `src/audio/engine.ts`: Tone.js。`previewNote` は専用シンセ、再生中は鳴らさない
- `tests/gridEntry.test.ts` / `tests/swing.test.ts`: `npm run test:entry` で実行

### 触るときの注意(今回の作業で分かったこと)

- StaffView の当たり判定・カーソル用の `rect`/`line` には必ず `stroke: none` を付ける(SVGルートの stroke を継承して枠が見える)。
- StaffView のクリック/ドラッグ系コールバックは ref で持ち、描画の deps に入れない。ドラッグのリスナーは `window` に付ける(描き直しで要素が消えるため)。
- 1行の小節数に「最低○小節」の下限を入れない(VexFlowは幅が足りないと小節外へはみ出して音符が重なる)。縮小の下限(0.6など)も入れない。
- スウィングは拍ごとに判定する。x.5 を無条件に遅らせると16分が崩れる。
- `GridEditor` のカーソル位置は `WeakMap<GridPhrase, …>` で grid オブジェクトの同一性に紐づく。親で grid をクローンして渡し直すと位置が失われる。
- `staffSizing.ts` の定数は `styles.css` の `.staff-sticky` の max-height を写している。片方を変えたら両方。
- 既知の制限: 1小節2コードの編集パレットは1つ目のコードのみ / 3連の拍境界をまたぐ音は不可 / 行またぎのタイは半タイ / スマホ縦で密な12小節は全体を収めると音符が小さい。

### オーナーとの協働

- やり取りは日本語。大きな設計変更は先に案を出して承認を得てから。質問には選択肢+推奨を添える。
- バグ報告は再現手順つきで来る。修正後は「原因・何を変えたか・どう検証したか」を具体的に報告する。
- 実機で音が確認できない環境の場合、再生系の変更は「音の実聴だけ未確認」と明記して渡す。
